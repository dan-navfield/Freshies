import { Stack } from 'expo-router';

export default function ChildRoutineLayout() {
    return (
        <Stack
            screenOptions={{
                headerShown: false,
            }}
        />
    );
}
